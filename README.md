# Template flask

## install lib
+ install flask
    + `pip install flask`
+ install colorama
    + `pip install colorama`

## log install 
+ create folder
    + create folder `log`
    + create `logs.txt` in `log` folder